#!/bin/bash

# Run a named hook, like post-update (available in ~/.config/prometheus/hooks/post-update).

set -e

if (( $# < 1 )); then
  echo "Usage: prometheus-hook [name] [args...]"
  exit 1
fi

HOOK=$1
HOOK_PATH="$HOME/.config/prometheus/hooks/$1"
shift

if [[ -f $HOOK_PATH ]]; then
  bash "$HOOK_PATH" "$@"
fi
