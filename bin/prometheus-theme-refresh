#!/bin/bash

# Refresh the current theme from its templates.

THEME_NAME_PATH="$HOME/.config/prometheus/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  prometheus-theme-set "$(cat $THEME_NAME_PATH)"
fi
